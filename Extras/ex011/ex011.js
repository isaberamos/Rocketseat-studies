let num = [5, 75, 98]

num.sort()

console.log(`O array tem ${num.length} elementos`)

console.log(`Na posição 1 temos o elemento ${num[1]}`)

for (let pos = 0; pos < num.length; pos++) {
    console.log(num[pos])
}